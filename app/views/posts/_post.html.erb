<div class=post-content>
  <h2><%= @post.title %></h2>
  <p><%= @post.content %></p>
  <ul>
    <% @post.categories.each do |category| %>
      <li><%= category.name %></li>
    <% end %>
  </ul>
</div>
